\# GitHub Fetcher CLI



A simple command-line tool to fetch and display GitHub user profiles. 

Built with Python and `httpx`.



\## Setup \& Execution (Windows)

1\. Ensure your virtual environment is active.

2\. Install dependencies: `pip install -r requirements.txt`

3\. Run the script: `python github\_fetcher.py`

